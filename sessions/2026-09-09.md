# Session — 2026-09-09

## Opener

- Picked up from 2026-09-08 session close
- Token still valid — no new token needed
- Goal: client key routing and beyond

---

## What was built

### Client key routing
- `page.tsx` now reads `?client=` from the URL and defaults to `demo`
- `AgentWorkspace.tsx` accepts `clientKey` as a prop and includes it in the POST body
- Decision: URL param now, env var per deployment in production

### Client config API route
- New `/api/client` route reads `clients/[clientKey]/config.json` and returns client name, domains, and current user
- `page.tsx` fetches this on load and uses real data — hardcoded `DEMO_CONFIG` removed

### Loading and error states
- Three explicit startup states: loading, error, ready
- Loading: quiet centered indicator
- Error: clear "Client not found" message naming the key that failed
- App holds full render until config is confirmed — no flicker

### User context in agent
- `assemblePrompt.ts` updated with a `UserContext` interface and role-aware system prompt section
- `route.ts` accepts `user` in the request body and passes it to `assembleSystemPrompt`
- `AgentWorkspace.tsx` accepts `user` prop and includes it in the POST body
- `page.tsx` passes full user object down from config
- Verified working: agent gave a correct content-owner response to override question

### Vercel deployment
- App deployed to Vercel Hobby
- Live at https://content-systems-platform.vercel.app/?client=demo
- Fixed Vercel build error: wrapped `useSearchParams()` in Suspense boundary
- ANTHROPIC_API_KEY set as environment variable in Vercel dashboard
- Auto-deploy on GitHub push confirmed working

---

## Decisions made

All logged in DECISIONS.md. Summary:

| Decision | Detail |
|---|---|
| Client key routing | URL param now, env var per deployment in production |
| One deploy per client | Confirmed as production architecture |
| Loading/error states | Built before moving on — app feels solid not fragile |
| Real config data in UI | Hardcoded values replaced with API-loaded data |
| User context in agent | Role and domains passed in system prompt invisibly |
| Authentication model | Username/password, credentials in env vars not repo |
| Client API keys | Per-client, stored as env var in their deployment |
| Initial password | Keri sets, user resets on first login |
| Session length | 12 hours |
| Non-engineer framing | Standing agreement — every change gets plain-language "so what" |
| Decision logging | Real-time during sessions |

---

## Ideas logged

- Social media post add-on — generates platform-appropriate posts from reviewed content

---

## Next session: start here

**Pick up with: authentication.**

What's decided:
- Username and password credentials
- Credentials stored as environment variables in Vercel (not in repo, not in config)
- Passwords hashed — never plain text
- Keri sets initial password during client setup
- User must reset password on first login
- Sessions last 12 hours
- Login endpoint must be rate-limited
- Credentials only over HTTPS (Vercel handles this automatically)

What to build (in order):
1. Password hashing utility — script Keri runs during setup
2. Login API route — /api/auth/login
3. Session API route — /api/auth/session
4. Login screen
5. Forced reset screen
6. Auth guard in page.tsx
