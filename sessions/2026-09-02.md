# Session Summary — 2026-09-02

## What we did

- Established the end-of-session workflow: session summaries committed to `/sessions/YYYY-MM-DD.md`, `PROJECT_STATE.md` updated at close, and a Session Opener block included at the bottom of each summary for easy next-session re-orientation
- Defined the base subject library — five mandatory subjects that every client instance inherits, each with explicit in-scope and out-of-scope statements
- Committed all five subject files to `base/subjects/`
- Updated `PROJECT_STATE.md` to reflect base subject library completion
- Resolved the voice and tone model: Keri-defined default ships with every client; customization is done collaboratively during discovery and replaces the default entirely for that instance
- Updated `base/subjects/voice-and-tone.md` to reflect that model

## Decisions made

| Decision | Detail |
|---|---|
| Session summary workflow | Summaries in `/sessions/YYYY-MM-DD.md`; `PROJECT_STATE.md` updated each session; Session Opener block included for next session |
| Five base subjects at launch | Plain language, accessibility, inclusive language, terminology governance, voice and tone |
| All base subjects mandatory | No opt-out; consistent quality floor for every client instance |
| Explicit in/out of scope per subject | Prevents client misunderstanding; sets clear agent boundaries |
| Voice and tone model | Keri-defined default active for all clients; replaced (not layered) by client-specific customization during discovery; customization is always collaborative, never self-service |
| Default voice and tone content | To be drafted in a future session |

## Problems encountered

- GitHub secret scanning is enabled on the repo, which may have flagged the token pasted in chat. Token appeared functional throughout the session, but should be regenerated before the next session as a precaution.

## Next session

- Regenerate GitHub personal access token before starting
- Draft the default voice and tone that ships with every client instance
- Begin defining the base prompt templates in `base/prompts/`

---

## Session Opener — for next session

Paste this after the session starter doc, then paste your GitHub token.

```
## Session opener — 2026-09-02

### Where we left off
Base subject library is complete — five mandatory subjects committed to `base/subjects/` with explicit in/out of scope statements. Voice and tone scope updated to reflect the Keri-defined default model. PROJECT_STATE.md is current.

### Goals for this session
- Draft the default voice and tone (Keri-defined, ships with every client)
- Begin base prompt templates in `base/prompts/`

### Open questions needing a decision
- Voice and tone: no outstanding questions — model is resolved
- Daily digest delivery method (email, Slack, or other) — not yet decided
- UI branding: neutral always, or light per-client customization?
- What triggers a post-handoff "new engagement" — size of change, governance tier affected, or client request?

### Housekeeping
- [ ] Regenerate GitHub token — previous token was pasted in chat and may have been flagged by secret scanning
- [ ] Paste new GitHub token
- [ ] Note any changes to the project since last session
```

