# Session Summary — 2026-09-08

## What we did

- Got the app running locally for the first time — cloned repo, scaffolded Next.js project files (package.json, next.config.mjs, tsconfig.json, tailwind.config.ts, postcss.config.mjs), added Anthropic API key, confirmed agent responding at localhost:3001.
- Fixed next.config.ts → next.config.mjs (this version of Next.js doesn't support .ts config).
- Built dynamic prompt assembly — assemblePrompt.ts in ui/app/lib/ reads base subject prompts, stage prompts, request handling prompt, client config, and voice and tone doc from the repo at runtime and assembles them into a single system prompt. Replaces the hardcoded system prompt in route.ts.
- Updated API route (route.ts) to call assemblePrompt() instead of using the hardcoded string. Accepts optional clientKey from the request body, defaults to 'demo'.
- Created demo client config — clients/demo/config.json. Acme Co., three domains (Marketing, Product, Legal), four users across all three roles. Legal domain set to no overrides permitted and lower flagging threshold.
- Verified full chain end to end — agent correctly reports Acme Co.'s three domains when asked.
- Added two ideas to IDEAS.md: client-facing platform explainer and leadership ROI documentation; client content creation path (initiation, development, human review) configured during discovery.
- Updated PROJECT_STATE.md.

## Decisions made

| Decision | Detail |
|---|---|
| Dynamic prompt assembly | assemblePrompt.ts assembles system prompt from repo files at runtime |
| Demo client | Acme Co. — Marketing, Product, Legal domains; used to verify full config-to-prompt chain |
| Legal domain constraints | No overrides permitted, lower flagging threshold — realistic constraint for that content type |

## Problems encountered

- next.config.ts not supported by this version of Next.js — replaced with next.config.mjs.
- Git push rejected due to remote commits (IDEAS.md updates from Claude) — resolved with git config pull.rebase false and git pull before push.
- Dev server occupying terminal — needed new terminal window for git commands. App moved to port 3001 when 3000 was occupied.
- API key accidentally shared in chat — revoked and replaced immediately.

## Next session

- Client key routing — pass client key from UI to API route so the correct config loads per user
- Begin building out remaining UI views: Requests, Logs, Domains, Admin
- Decide digest delivery method (email vs. Slack) — still blocking digest build

---

## Session Opener — for next session

Paste your GitHub token. If today is a new day (after 2026-09-08), regenerate it first.

### Where we left off
App is running locally at localhost:3001. Dynamic prompt assembly is working — assemblePrompt.ts reads from repo files at runtime. Demo client config (Acme Co.) is in clients/demo/config.json. Full chain verified: agent correctly references client domains from config. PROJECT_STATE.md and IDEAS.md are current.

### Goals for this session
- Client key routing — pass client key from UI to API route
- Begin remaining UI views (Requests, Logs, Domains, Admin)
- Decide digest delivery method

### Open questions needing a decision
- Digest delivery method — email, Slack, or other (blocking digest build)
- UI branding — neutral always, or light per-client customization?
- What triggers a post-handoff new engagement?
- Stakeholder relationship types beyond "informed"

### Housekeeping
- If today is a new day (after 2026-09-08): regenerate GitHub token and paste it now
- If same day: paste existing token
- Note any changes to the project since last session
